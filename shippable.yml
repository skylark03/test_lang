language: php

env:
  global:
    # Hipchat config
    - PROJECT="Some project"
  matrix:
    - IMAGE=php7.0
    - IMAGE=php7.1
    
matrix:
  include:
    - php: "7.0"
      env: IMAGE=php7.0
    - php: "7.1"
      env: IMAGE=php7.1

build:
  pre_ci:
    - env | grep IMAGE
  ci:
    - echo "Hello, world"
